// Placeholder for an optional Plane (project management) integration.
// Nothing in the pipeline depends on this yet — GitHub issues/sub-issues
// are the system of record (§4.2). Fill in if/when Plane is actually wired
// up as a secondary tracker; until then this file intentionally does nothing.
export {};
